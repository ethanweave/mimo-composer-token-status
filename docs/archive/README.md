# Archive

Legacy / non-shipping material. Not used by `npm run connect` or `npm run verify`.

| Item | Why archived |
|------|----------------|
| `PHASE2.md`, `PHASE3.md` | Superseded by v2.0.0 release docs |
| `overlay/` | Optional OS overlay experiment; not the product path |
| `patch-asar.mjs.unused` | **Forbidden** for shipping — would modify `app.asar`. Kept only as historical reference; do not run. |

Do not restore `patch-asar` into the supported surface.
